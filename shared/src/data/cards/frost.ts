/**
 * Frost — The Wildcard.
 *
 * Card advantage and board manipulation, expressed through ice: Chill and
 * Freeze deny tempo, and the class wins by outplaying rather than out-statting.
 *
 * Constraint (§6): must not encroach on the reserved classes — no resource
 * generation (Electricity) and no permanent stat boosting (Nature).
 *
 * See _examples.ts for the syntax of each card shape.
 */

import type { DeckCard } from '../../types/card';

export const frostCards: DeckCard[] = [];